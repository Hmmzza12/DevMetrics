/**
 * One colour per side, used consistently across every paired visualization so a
 * reader can tell the two users apart without re-reading each legend. Indigo is
 * the product primary; amber is the furthest distinguishable hue from it on the
 * dark surface (including for red/green colour blindness).
 */
export const SIDE_A_COLOR = '#6366f1';
export const SIDE_B_COLOR = '#f59e0b';
